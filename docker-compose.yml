version: '3.8'

services:
  kitsune:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kitsune-prod
    ports:
      - "3000:3000"
    environment:
      - KATAGO_BINARY=/usr/local/bin/katago
      - KATAGO_NETS_PATH=/app/nets
      - KITSUNE_CONFIG_DIR=/app/configs
      - RUST_LOG=info
      - ENV=production
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    volumes:
      # Optional: persist logs
      - ./logs:/app/logs
