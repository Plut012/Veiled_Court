# KataGo Analysis Engine config — post-game review
# Tuned for batch analysis of full games on RTX 2070

# Logs
logDir = analysis_logs

# Analysis
reportAnalysisWinratesAs = BLACK

# Search limits — 200 visits is fast (~0.5s/position) with reasonable accuracy
maxVisits = 200

# Threading — batch many positions in parallel
numAnalysisThreads = 8
numSearchThreadsPerAnalysisThread = 2

# GPU batching
nnMaxBatchSize = 64

# Cache
nnCacheSizePowerOfTwo = 23
nnMutexPoolSizePowerOfTwo = 17
nnRandomize = true
